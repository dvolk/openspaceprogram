# Ship switch: load the 7-ship fleet (each ships with its crew aboard, so
# 14 fleet entries); the active ship starts as #1 (racer) and one F6
# advances past the racer's crew to #3 (tanker). Verifies the fleet + the
# F6 command path (which skips the non-controllable aboard crew).
NAME ship-switch
ARGS --fleet res/fleet.json --time-accel 1 --timeout 8
ARGS --sim-press 1500,50,F6
EXPECT Active ship 3 of 14: tanker
FORBID GL_
FORBID error:
