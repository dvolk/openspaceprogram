# Staging: on the pad, two SPACE presses should drop the stager's parts and
# then report nothing left. Verifies the stage-separation path end to end.
# Anchors are the exact strings the staging handler prints.
NAME staging-basic
ARGS --scenario pad --ship res/ships/stager.json --time-accel 1 --timeout 12
ARGS --sim-press 2000,100,SPACE
ARGS --sim-press 6000,100,SPACE
EXPECT Stage: dropped
EXPECT nothing left to separate
FORBID GL_
FORBID error:
