# The gamma/brightness postfx, standalone (composites straight to the
# screen). Must load, run without GL errors, and log in the requested order.
NAME postfx-gamma
ARGS --postfx gamma --gl-debug --timeout 4
EXPECT postfx: gamma
EXPECT Main loop starting
FORBID GL_
FORBID error:
FORBID Error compiling shader
FORBID Error linking shader program
