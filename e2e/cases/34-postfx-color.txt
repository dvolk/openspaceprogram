# The color postfx (gamma/brightness/black level/saturation), standalone
# (composites straight to the screen). Must load, run without GL errors,
# and log in the requested order.
NAME postfx-color
ARGS --postfx color --gl-debug --timeout 4
EXPECT postfx: color
EXPECT Main loop starting
FORBID GL_
FORBID error:
FORBID Error compiling shader
FORBID Error linking shader program
